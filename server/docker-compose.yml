services:
  db:
    image: postgres:16
    container_name: focusos-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: focusos
    ports:
      - "5432:5432"
    volumes:
      - focusos_db:/var/lib/postgresql/data

volumes:
  focusos_db:

